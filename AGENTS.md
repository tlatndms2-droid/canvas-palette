# Closed-loop update rule

For every approved implementation, verification, update, or Release request in this repository, read and follow `C:\Users\tlatn\.codex\skills\closed-loop-update\SKILL.md` before taking task actions.

This repository uses an approved-behavior allowlist. Do not add user-visible behavior, shortcuts, alternate workflows, or validation targets outside that allowlist. Recoverable failures must be fixed and revalidated internally; do not stop to narrate them. When Release is requested, publish through the established route after verification passes and report only the completed result.
