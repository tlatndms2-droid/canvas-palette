# UI specification

Card, Markdown, Image, and Group retain distinct presentation. Side Palette regions scroll independently. Mini Palette Collect opens review details by selection, while Storage uses docked panes. Click selection controls persistent preview; Ctrl+hover is reserved for temporary preview without selection changes.
