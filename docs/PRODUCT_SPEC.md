# Product specification

Canvas Palette provides a three-stage workflow: collect Canvas assets, organize them in workspace palettes, and reuse them across canvases. Its four first-class item types are Card, Markdown, Image, and Group. Text scraps and direct memos become Cards.

The Side Palette is a formal workspace-specific organizer. Mini Palette is a global hub with separate Collect and Storage tabs. Storage uses independent docked left control, main asset, and right preview panes.
